# Put these rules BEFORE Chatwoot's catch-all ProxyPass rule in the HTTPS vhost
# for support.rocobroker.com, then rebuild cPanel includes and reload Apache.
ProxyPass        /agent-bot/ http://127.0.0.1:3200/ retry=0 timeout=40
ProxyPassReverse /agent-bot/ http://127.0.0.1:3200/
